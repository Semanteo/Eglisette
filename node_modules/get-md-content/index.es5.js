'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) arr2[i] = arr[i]; return arr2; } else { return Array.from(arr); } }

var _commonmarkHelpers = require('commonmark-helpers');

var _ramda = require('ramda');

var wrap = function wrap(item) {
  return (0, _ramda.is)(String, item) || (0, _ramda.is)(RegExp, item) ? function (node) {
    return (0, _commonmarkHelpers.text)(node).match(item);
  } : item;
};

exports['default'] = function (input) {
  var removeList = arguments.length <= 1 || arguments[1] === undefined ? [] : arguments[1];

  var node = _commonmarkHelpers.matchRemoveList.apply(undefined, [input].concat(_toConsumableArray(removeList.map(wrap).filter((0, _ramda.is)(Function)))));
  if (!node) return;
  return {
    text: (0, _commonmarkHelpers.text)(node),
    html: (0, _commonmarkHelpers.html)(node),
    node: node
  };
};

module.exports = exports['default'];

